---
// Custom hero — overrides Starlight's default. Drenched navy ground spanning
// the first viewport, identical in light and dark mode (the hero is its own
// thing, per the design brief). Reads page frontmatter for tagline and CTAs.
import { Icon } from '@astrojs/starlight/components';
import iconImage from '../assets/icon.svg';

const { entry } = Astro.locals.starlightRoute;
const { hero = {}, title: pageTitle } = entry.data;
const { title = pageTitle, tagline, actions = [] } = hero;
---

<section class="pf-hero">
	<div class="pf-hero-inner">
		<img
			class="pf-hero-icon"
			src={iconImage.src}
			alt=""
			width="180"
			height="180"
			loading="eager"
			decoding="sync"
		/>
		<h1 class="pf-hero-title">{title}</h1>
		{tagline && <p class="pf-hero-tagline">{tagline}</p>}
		{actions.length > 0 && (
			<div class="pf-hero-actions">
				{actions.map((action) => (
					<a
						href={action.link}
						class:list={['pf-hero-cta', `pf-hero-cta--${action.variant ?? 'primary'}`]}
					>
						<span>{action.text}</span>
						{action.icon && <Icon name={action.icon} size="1em" />}
					</a>
				))}
			</div>
		)}
	</div>
	<div class="pf-hero-glow" aria-hidden="true"></div>
</section>

<style>
	.pf-hero {
		position: relative;
		isolation: isolate;
		margin: -1.5rem -1.5rem 2.5rem;
		padding: clamp(3rem, 8vw, 6rem) clamp(1.5rem, 4vw, 3rem);
		min-height: min(72vh, 640px);
		display: grid;
		place-items: center;
		text-align: center;
		overflow: hidden;
		background:
			radial-gradient(ellipse at top, var(--pf-navy-500) 0%, var(--pf-navy-700) 45%, var(--pf-navy-900) 100%);
		color: var(--pf-paper-100);
		border-radius: 0;
	}

	@media (min-width: 50rem) {
		.pf-hero {
			margin-left: -3rem;
			margin-right: -3rem;
			border-radius: 12px;
		}
	}

	.pf-hero-inner {
		position: relative;
		z-index: 1;
		max-width: 44rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1.25rem;
	}

	.pf-hero-icon {
		filter: drop-shadow(0 18px 40px rgba(4, 8, 14, 0.45));
	}

	.pf-hero-title {
		font-family: 'Fraunces Variable', 'Fraunces', Georgia, serif;
		font-variation-settings: 'opsz' 144, 'SOFT' 50, 'WONK' 1;
		font-weight: 600;
		font-size: clamp(2.75rem, 1.8rem + 4.5vw, 5rem);
		line-height: 0.95;
		letter-spacing: -0.025em;
		color: var(--pf-paper-50);
		margin: 0;
	}

	.pf-hero-tagline {
		font-size: clamp(1.05rem, 0.95rem + 0.4vw, 1.25rem);
		line-height: 1.45;
		color: color-mix(in oklch, var(--pf-paper-100) 88%, transparent);
		max-width: 32rem;
		margin: 0;
	}

	.pf-hero-actions {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.75rem;
		margin-top: 0.5rem;
	}

	.pf-hero-cta {
		display: inline-flex;
		align-items: center;
		gap: 0.5em;
		padding: 0.7em 1.4em;
		border-radius: 999px;
		font-weight: 500;
		font-size: 0.95rem;
		letter-spacing: 0.005em;
		text-decoration: none;
		transition: transform 140ms ease, background 140ms ease, color 140ms ease, box-shadow 140ms ease;
	}

	.pf-hero-cta--primary {
		background: var(--pf-amber-400);
		color: var(--pf-navy-900);
		box-shadow: 0 8px 20px -8px rgba(240, 192, 96, 0.6);
	}

	.pf-hero-cta--primary:hover {
		background: var(--pf-amber-300);
		transform: translateY(-1px);
	}

	.pf-hero-cta--secondary,
	.pf-hero-cta--minimal {
		background: transparent;
		color: var(--pf-paper-100);
		box-shadow: inset 0 0 0 1px color-mix(in oklch, var(--pf-paper-100) 30%, transparent);
	}

	.pf-hero-cta--secondary:hover,
	.pf-hero-cta--minimal:hover {
		background: color-mix(in oklch, var(--pf-paper-100) 8%, transparent);
		box-shadow: inset 0 0 0 1px color-mix(in oklch, var(--pf-paper-100) 50%, transparent);
	}

	/* Subtle amber glow drifting behind the icon — reads as warmth, not decoration. */
	.pf-hero-glow {
		position: absolute;
		inset: auto auto -20% 50%;
		width: min(80vw, 720px);
		aspect-ratio: 1.4;
		transform: translateX(-50%);
		background: radial-gradient(circle, color-mix(in oklch, var(--pf-amber-500) 35%, transparent) 0%, transparent 60%);
		filter: blur(40px);
		opacity: 0.45;
		z-index: 0;
		pointer-events: none;
	}

	@media (prefers-reduced-motion: reduce) {
		.pf-hero-cta {
			transition: none;
		}
	}
</style>
