// Windows resource script. Embeds resources/icons/icon.ico into
// PortFinder.exe's PE resource section. Resource ID 1 (the
// IDI_APPLICATION convention) is what Windows looks up first when
// assigning a window / shortcut icon for an executable.
//
// Path is relative to this .rc file (embed-resource convention).

1 ICON "icons/icon.ico"
